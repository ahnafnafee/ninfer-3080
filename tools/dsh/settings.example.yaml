# Merge these fields into the DSH home's settings.yaml, preserving other fields.
agent-default-model:
  provider: qwen-3080
  model: bonsai2-heretic
  reasoningEffort: medium
agent-presets:
  default: bonsai2-heretic-3080
